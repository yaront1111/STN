Build the CHIMERA project and run all unit tests.

Steps:
1. Build the chimera_tests target using cmake
2. Run the tests with verbose output
3. If any tests fail, show the failure details and suggest fixes
4. Report the build time and test results summary
